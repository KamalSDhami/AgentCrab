# Mission Control (local demo)

This folder is a local/demo filesystem Mission Control so the dashboard can run on Windows.

On the VPS, the real Mission Control lives at:

- `/root/.openclaw/workspace/mission_control/`
